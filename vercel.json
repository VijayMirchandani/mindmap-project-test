{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    { "src": "/mindmap.json", "dest": "/public/mindmap.json" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
